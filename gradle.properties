org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Gradle构建优化
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Kotlin编译优化
kotlin.incremental=true
kotlin.incremental.android=true

# AGP 9.0+ 默认值 (已移除过时设置)
# - android.newDsl=true (default)
# - android.builtInKotlin=true (default, uses built-in Kotlin support)
# - android.enableAppCompileTimeRClass=true (default)
# - android.enableJetifier=false (default, no longer needed)
# - android.r8.optimizedResourceShrinking=true (default)